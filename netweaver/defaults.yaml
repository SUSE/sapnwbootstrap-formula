netweaver:
  clean_nfs: True
  installation_folder: /tmp/unattended
  additional_dvds: []
  # Set this entry to false to disable creating swap
  create_swap:
      # Path containing the swapfile
      directory: /var/lib/swap
      # Swap size in MB
      size: 2048
